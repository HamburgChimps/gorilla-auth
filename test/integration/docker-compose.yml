version: '3.1'
services:
  gorilla-server:
    build: ../../../
    ports:
     - "9000:9000"
    volumes:
      - ../../:/app
    working_dir: /app
    command: ["node_modules/.bin/mocha", "test/integration/**/*.js", "--timeout", "5000", "--colors", "--exit"]
